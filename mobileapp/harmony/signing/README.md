# 本地签名目录

此目录只放您本人从华为开发者服务获得的真实证书、私钥和 Profile。

- 不要把签名文件提交到 Git 或发送给其他人。
- 不要使用网上共享证书或伪造证书。
- 在 DevEco Studio 的 `File > Project Structure > Signing Configs` 中配置自动签名或发行签名。
- 真机安装要求 HAP 已签名；模拟器和预览器不要求签名。

根目录 `.gitignore` 已屏蔽常见签名文件扩展名和本目录中的私密内容。
